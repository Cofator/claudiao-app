# claudio-app

Cockpit web do Claudião (app.claudiao.app). Vite + React + shadcn/ui.

## Scripts

- `npm run dev` — dev server (http://localhost:5173)
- `npm run build` — build de produção (saída em `dist/`)
- `npm run preview` — preview do build
- `npm test` — roda testes
- `npm run typecheck` — type check sem emit
- `npm run build:bridge` — empacota a Bridge em `.zip`

## Estrutura

Ver `docs/superpowers/specs/2026-06-16-claudiao-app-fase1b-cockpit-design.md` no repo `claudiao-backend`.
